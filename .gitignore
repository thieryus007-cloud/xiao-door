# nRF Connect SDK / Zephyr build artifacts
build/
*.uf2
*.hex
*.elf
*.map
.west/

# SDK toolchain (installed locally, not versioned)
toolchain/
ncs/

# OS
.DS_Store

# Claude Code local settings (machine-specific, not for sharing)
.claude/

# Données de commissioning Matter par unité (passcode = secret, jamais sur GitHub public)
# voir firmware/apps/lock/generate_unit_secrets.py
firmware/apps/lock/unit-secrets/

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
